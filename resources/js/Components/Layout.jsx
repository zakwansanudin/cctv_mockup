/**
 * Layout — backwards-compatible re-export of AppLayout.
 *
 * All pages import from './Components/Layout', so keeping this
 * file means zero changes are needed in any Page component.
 *
 * If you need the full shell directly, import AppLayout instead.
 */
export { default } from './AppLayout';
